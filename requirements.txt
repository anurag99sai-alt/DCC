# Competition Full Workflow Demo

This folder contains an example workflow for the Data Creation competition.

## Setup

1. Copy `.env.example` to `.env` and fill in your credentials.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the notebook: `jupyter notebook DataCreationCompetition_workflow.ipynb`
